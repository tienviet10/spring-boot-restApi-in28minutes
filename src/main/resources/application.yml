logging:
  level:
    org.springframework.web: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    defer-datasource-initialization: true
    show-sql: true
  security:
    user:
      name: username
      password: password